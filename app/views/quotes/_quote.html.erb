<%= quote.quoted_person %> said, <q><%= quote.body %></q>, in the <%= quote.location %>
<% if quote.created_by == current_user %>
  <%= link_to "delete quote", quote_path(quote), method: :delete  %>
  <%= link_to "edit quote", edit_quote_path(quote) %>
<% end %>
<%= render 'votes/voting', quote: quote, vote: vote %>
<div>
  Quote Love: <%= quote.score %>
</div>
